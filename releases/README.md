# Stable release notes

Stable releases use a signed annotated `v<major>.<minor>.<patch>` tag and a
matching `releases/v<version>.md` notes file. Test-channel builds are published
without a semantic-version tag and are consumed through the `release-catalog`
branch. Keep version-specific notes out of this file; this document only records
the publication contract used by CI.
