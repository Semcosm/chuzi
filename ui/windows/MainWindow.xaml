<Window
    x:Class="Chuzi.Native.Windows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
  <Grid Padding="28" RowSpacing="16" ColumnSpacing="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <TextBlock Text="Chuzi" FontSize="28" FontWeight="SemiBold" />
    <TextBlock Grid.Row="1" Text="Core request control" Opacity="0.72" />
    <TextBox x:Name="AccountIdBox" Grid.Row="2" Header="Account ID" PlaceholderText="account-1" />
    <StackPanel Grid.Row="3" Orientation="Horizontal" Spacing="10">
      <Button x:Name="SubmitButton" Content="Submit" Click="SubmitButton_Click" />
      <Button x:Name="RefreshButton" Content="Refresh" Click="RefreshButton_Click" />
      <Button x:Name="CancelButton" Content="Cancel" Click="CancelButton_Click" IsEnabled="False" />
    </StackPanel>
    <ScrollViewer Grid.Row="4" Margin="0,8,0,0">
      <TextBlock x:Name="StatusText" TextWrapping="Wrap" IsTextSelectionEnabled="True" />
    </ScrollViewer>
  </Grid>
</Window>
