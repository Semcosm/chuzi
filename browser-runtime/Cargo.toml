[package]
name = "chuzi-browser-runtime"
version = "0.1.0"
edition = "2021"
publish = false
description = "Protocol boundary for the chuzi browser runtime helper"
license = "Apache-2.0"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
